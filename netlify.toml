# Netlify Configuration
# Frontend Only Deployment

[build]
  # Client klasöründen build yap
  base = "client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Secrets scanning - dokümantasyon ve Vite build output'u hariç tut
  SECRETS_SCAN_OMIT_PATHS = "*.md,*.example,client/.env.example,backend/.env.example,.netlify/**,dist/**"
  # Vite environment variables bundle'a gömülür, normal davranış
  SECRETS_SCAN_OMIT_KEYS = "VITE_API_URL,VITE_ADMIN_PASSWORD,VITE_ADMIN_PATH"

# SPA routing için - tüm route'ları index.html'e yönlendir
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Performance - Static dosyalar için cache
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
